New: Added BoundingBox::get_neighbor_type() which , given another BoundingBox, returns the enum type NeighborType. Added the function GridTools::compute_mesh_predicate_bounding_box() in grid_tools which returns a vector of BoundingBoxes containing the cells for which a given predicate is true.
<br>
(Giovanni Alzetta, 2017/10/20)
